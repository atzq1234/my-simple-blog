<!-- 导航条 -->
<header>
    <nav class="navbar navbar-inverse m-header" id="navbar">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="/"> <img src="/images/kato.jpg" alt="" style="width: 20px;height: 20px;"/></a>
            </div>
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li><a href="/">博客</a></li>
                    {{#if isAdmin}}
                        <li><a href="/admin/ctrl-article">里世界</a></li>
                    {{/if}}
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li>
                        <form action="/search" method="get" class="form-inline u-ipt-search">
                            <div class="form-group">
                                <input type="text" class="form-control" name="title" placeholder="搜索点什么..."/>
                                <span class="glyphicon glyphicon-search" id="btnSearch"></span>
                                <input type="submit" />
                            </div>
                        </form>
                    </li>
                    {{#if isLogin}}
                        <li><a href="javascript:;">{{username}}</a></li>
                        <li><a href="javascript:;" id="btnSignUp">退出</a></li>
                    {{else}}
                        <li><a href="#" data-toggle="modal" data-target="#modalLogin">登录</a></li>
                        <li><a href="#" data-toggle="modal" data-target="#modalRegister">注册</a></li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>
</header>